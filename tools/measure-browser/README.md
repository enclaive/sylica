# Measurement in the Browser

Calculate SEV-SNP measurements with a web browser using drag-and-drop and live reload.

## Development

Dependencies are managed by vendoring bundles:

```bash
./vendor.sh
```

Start a simple web server:

```bash
python server.py
```

Visit to see your result [http://localhost:8080](http://localhost:8080).